# Personal site

A simple one-page personal website: plain HTML and CSS, no build step.

- `index.html` is the page content. Edit the text and swap placeholders for real images and videos.
- `style.css` controls the look: colors, spacing, and the gallery layout.

To add media, create `images/` and `videos/` folders and follow the comments in `index.html`.
